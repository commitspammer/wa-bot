<!doctype html>
<div class="space-y-2">
    <% if (messages.length === 0) { %>
        <span>
            There are no messages
        </span>
    <% } else { %>
        <% for (m of messages) { %>
            <%- include('comps/message.ejs', { message: m }) %>
        <% } %>
    <% } %>
</div>
